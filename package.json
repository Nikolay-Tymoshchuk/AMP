{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {},
  "devDependencies": {
    "lerna": "8.1.2"
  },
  "scripts": {
    "start": "lerna exec --scope server -- yarn run dev & lerna exec --scope client -- yarn run dev"
  }
}
